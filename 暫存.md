# 管理員營運控制台實作追蹤

使用者於 2026-09-13 要求完整處理稽核發現，並明確要求建立本暫存追蹤檔。完成項目必須有驗證證據，不以介面存在代替後端生效。

## 約束

- 校園共用 IP：IP 僅作寬鬆異常流量保護，已登入操作主要依 UID 計算。
- 前端可採較緊節流；後端仍執行權威配額，所有可調政策必須有真正 consumer。
- ADMIN_EMAILS 保持唯一平台總管理員來源；credential 不進 browser。
- 既有 migration 不修改；以新 migration 完成 schema/RPC 更新。
- 保留現有 UI、路由與資料路徑。未連線的外部資源必須如實顯示未設定，不能顯示假的健康或用量。

## 工作清單

- [x] 重讀 AGENTS.md、structure.md，確認 main 工作樹乾淨。
- [x] 修正 Queue policy/retention batch 真正執行、claim/fencing、錯誤回報與進度。
- [x] 新增 domain event/operation 保存生命週期，避免 FK 導致永久保存；處理失敗刪除工作保存。
- [x] 動態 UID 配額、前端節流、寬鬆 IP 保護、DO 過期清理與業務拒絕彙總。
- [x] 動態內容、圖片、request、cache、realtime、job 政策與後端上下限。
- [x] 管理員政策編輯、前後差異／設定歷史、retention 預估影響、分頁與自訂限制時間。
- [x] 資料庫 table/index（含 TOAST）容量與每日歷史、清理預估、工作及錯誤中心。
- [x] Worker/provider 診斷、Cloudinary 用量、備份 workflow／artifact 與 server-side 整合。正式供應商 token 與部署未執行。
- [x] 媒體 cache 政策、Notion 封存排程與已知未完成刪除的精簡 backlog。歷史上已丟失 mapping 的外部副本不能推測刪除。
- [x] 真實 consumer、權限拒絕/跨 scope、quota 邊界、SQLite DO 並發／alarm 與校園共用 IP 測試。
- [x] generate:all、database contracts、verify:fast、verify:integration、verify:local、E2E。
- [x] 更新 structure.md、正式 docs、env examples，彙整已驗證/外部尚待設定的範圍；手機 HTML 說明頁已更新。

## 目前進度

已建立 0020–0029 migration；56 個新營運政策欄位，加上既有 retention 29／圖片 7，共 92 個 runtime 欄位。2026-09-13 最終 verify:integration 通過 43 tests、20 consistency checks 和 database contract drift；verify:local 13 階段通過；完整 E2E 44/44 通過。首次 E2E 暴露的快速雙次互動已改成有界排隊並補單元測試，兩條原先失敗流程都重跑成功。390px／1440px 營運頁實際截圖和儲存稽核已通過，Impeccable detector 回傳空 findings。新增測試使用的 Miniflare／esbuild 已明確鎖定原先實測版本，沒有升級執行期。

未連線 production、未 commit/push/deploy。外部診斷需要 server-side token；備份 workflow 來源已接上 runtime 政策，但未在正式 GitHub 執行。保留 ADMIN_EMAILS、演算法、provider protocol、原生安全 breaker 與部署排程等平台邊界，不建立假 UI 開關。

Build budget 非阻擋提醒：CSS 755.3 / 768 KiB，JS 2,789.2 / 3,072 KiB；未調高門檻，保留既有設計資產。UI architecture 原有 4 個 >300 行警告仍在 session-store、use-issue-detail、use-issue-feed、notifications；沒有新增 >400 行前端模組。

手機說明：http://100.123.185.127:8765/implementation.html；server 確認綁定 0.0.0.0:8765，Tailscale 地址 HTTP 200。

## 執行中發現

- Queue consumer 會忽略 DB 完成/失敗錯誤，已改成傳遞失敗。
- 原 maintenance log 讀取已退休的 outbox/deletion key，已改為 deliveries/jobs。
- 單批 policy 完成不代表整個 queue 完成，已查詢剩餘工作決定是否繼續。
- Integration 原先即使不啟動 frontend 也占用檢查 3000，會阻擋同機 Lexiro；已只檢查本次需要的 ports，未停止其他專案。
- 舊背景／delivery 完成 RPC 不驗證 claim，已改成每次由 DB 指派新 token；手動 retry 後舊 token 也必須拒絕。
- 清理失敗刪除 log 會丟掉外部物件識別碼，已新增精簡 backlog 與管理員重試。
- 公開媒體原先 browser 365 天 immutable、edge 30 天，已改成 runtime 政策（預設各 60 秒，上限 1 小時）；既有使用者已下載／已快取的副本無法追溯收回。
- Delivery 收件人讀取／通知寫入原先也吞 DB error，已補檢查，真實故障注入確認 delivery 保持 failed。
- Operation 大型 response 到期壓縮後回 operation-expired，不空回成功，不重做寫入。
- 測試用新 port 3100 時，auth probe 的 Origin 原先仍固定 3000；已與 runner 的 app URL 一致。
